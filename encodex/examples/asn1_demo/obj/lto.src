	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_main
_main:
	ld	hl, -198
	call	__frameset
	ld	bc, -186
	lea	iy, ix
	add	iy, bc
	ld	bc, -195
	lea	hl, ix
	add	hl, bc
	ld	(hl), iy
	lea	hl, ix - 96
	push	ix
	ld	bc, -189
	add	ix, bc
	ld	(ix), hl
	pop	ix
	lea	hl, iy
	ld	bc, -192
	lea	iy, ix
	add	iy, bc
	ld	(iy), hl
	ld	(ix - 96), 0
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	iy, (hl)
	inc	iy
	lea	de, iy
	lea	iy, ix
	add	iy, bc
	ld	hl, (iy)
	ld	bc, 89
	ldir
	ld	bc, -195
	lea	hl, ix
	add	hl, bc
	ld	iy, (hl)
	ld	(iy), 0
	ld	bc, -192
	lea	iy, ix
	add	iy, bc
	ld	hl, (iy)
	push	hl
	pop	iy
	inc	iy
	lea	de, iy
	ld	bc, 89
	ldir
	ld	hl, _.str
	push	hl
	ld	hl, -327680
	push	hl
	call	_sprintf
	pop	hl
	pop	hl
	ld	hl, 10
	push	hl
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	hl, (hl)
	push	hl
	ld	hl, 162
	push	hl
	ld	hl, _asn1_demo
	push	hl
	call	_cryptx_asn1_decode
	pop	de
	pop	de
	pop	de
	pop	de
	ld	bc, -195
	lea	iy, ix
	add	iy, bc
	ld	(iy), hl
	push	hl
	ld	hl, _.str.1
	push	hl
	ld	hl, -327680
	push	hl
	call	_sprintf
	pop	hl
	pop	hl
	pop	hl
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	iy, (hl)
	lea	iy, iy + 3
	or	a, a
	sbc	hl, hl
	ex	de, hl
BB0_1:
	ld	bc, -195
	lea	hl, ix
	add	hl, bc
	ld	hl, (hl)
	or	a, a
	sbc	hl, de
	jr	z, BB0_3
	or	a, a
	sbc	hl, hl
	ld	l, (iy - 3)
	push	ix
	ld	bc, -189
	add	ix, bc
	ld	(ix), de
	pop	ix
	ld	de, (iy)
	ld	bc, (iy + 3)
	push	bc
	push	de
	push	hl
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	hl, (hl)
	push	hl
	ld	hl, _.str.2
	push	hl
	ld	hl, -327680
	push	hl
	ld	bc, -198
	lea	hl, ix
	add	hl, bc
	ld	(hl), iy
	call	_sprintf
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	de, (hl)
	ld	bc, -198
	lea	iy, ix
	add	iy, bc
	ld	iy, (iy)
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	inc	de
	lea	iy, iy + 9
	jr	BB0_1
BB0_3:
	ld	hl, (ix - 90)
	push	hl
	ld	hl, _.str.3
	push	hl
	ld	hl, -327680
	push	hl
	call	_sprintf
	pop	hl
	pop	hl
	pop	hl
	ld	hl, (ix - 72)
	ld	de, (ix - 75)
	ld	bc, 10
	push	bc
	ld	(ix - 3), de
	ld	de, -192
	lea	iy, ix
	add	iy, de
	ld	bc, (iy)
	push	bc
	ld	de, (ix - 3)
	push	de
	push	hl
	call	_cryptx_asn1_decode
	pop	de
	pop	de
	pop	de
	pop	de
	ld	bc, -189
	lea	iy, ix
	add	iy, bc
	ld	(iy), hl
	push	hl
	ld	hl, _.str.1
	push	hl
	ld	hl, -327680
	push	hl
	call	_sprintf
	pop	hl
	pop	hl
	pop	hl
	ld	bc, -192
	lea	hl, ix
	add	hl, bc
	ld	iy, (hl)
	lea	iy, iy + 3
	or	a, a
	sbc	hl, hl
	ex	de, hl
BB0_4:
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	hl, (hl)
	or	a, a
	sbc	hl, de
	jr	z, BB0_6
	or	a, a
	sbc	hl, hl
	ld	l, (iy - 3)
	push	ix
	ld	bc, -192
	add	ix, bc
	ld	(ix), de
	pop	ix
	ld	de, (iy)
	ld	bc, (iy + 3)
	push	bc
	push	de
	push	hl
	ld	bc, -192
	lea	hl, ix
	add	hl, bc
	ld	hl, (hl)
	push	hl
	ld	hl, _.str.2
	push	hl
	ld	hl, -327680
	push	hl
	ld	bc, -195
	lea	hl, ix
	add	hl, bc
	ld	(hl), iy
	call	_sprintf
	ld	bc, -192
	lea	hl, ix
	add	hl, bc
	ld	de, (hl)
	ld	bc, -195
	lea	iy, ix
	add	iy, bc
	ld	iy, (iy)
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	inc	de
	lea	iy, iy + 9
	jr	BB0_4
BB0_6:
	or	a, a
	sbc	hl, hl
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	section	.data,"aw",@progbits
	public	_asn1_demo
_asn1_demo:
	db	"0Åü0", 13, "	*ÜHÜ˜", 13, "", 0, "Åç", 0, "0ÅâÅÅ", 0, "¿<†æ∞db¸.çù¡ß«ŒàçÖájAìE%#%8tŒOÒFı;î≤m¸†Fd∆≤3w/πâ3jŒÑäZˇà81Ê›–ÆÜ˝ıŸ%OÇì§Ã2""g¢hπ÷Æ‰≤ÓÄì±J+Ä''˝ôê∂‚ó*Q s6AR‹®ËiD	", 0, ""

	section	.rodata,"a",@progbits
	private	_.str
_.str:
	db	10, 10, "----------------------------------", 10, "ENCODEX ASN.1 Decoder Demo", 10, 0

	section	.rodata,"a",@progbits
	private	_.str.1
_.str.1:
	db	10, "Decode complete, %u objects parsed.", 10, 0

	section	.rodata,"a",@progbits
	private	_.str.2
_.str.2:
	db	"Obj %u, Tag Id: %u, Size: %u, Addr: %u", 10, 0

	section	.rodata,"a",@progbits
	private	_.str.3
_.str.3:
	db	"%s", 10, 0

	ident	"clang version 15.0.0 (https://github.com/jacobly0/llvm-project 72d82503a68765fd5a06d2026984f1fa399a54ea)"
	extern	__Unwind_SjLj_Register
	extern	__frameset
	extern	__Unwind_SjLj_Unregister
	extern	_sprintf
	extern	_cryptx_asn1_decode
