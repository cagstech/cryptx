	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_main
_main:
	ld	hl, -198
	call	__frameset
	ld	bc, -186
	lea	iy, ix
	add	iy, bc
	ld	bc, -195
	lea	hl, ix
	add	hl, bc
	ld	(hl), iy
	lea	hl, ix - 96
	push	ix
	ld	bc, -189
	add	ix, bc
	ld	(ix), hl
	pop	ix
	lea	hl, iy
	ld	bc, -192
	lea	iy, ix
	add	iy, bc
	ld	(iy), hl
	ld	(ix - 96), 0
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	iy, (hl)
	inc	iy
	lea	de, iy
	lea	iy, ix
	add	iy, bc
	ld	hl, (iy)
	ld	bc, 89
	ldir
	ld	bc, -195
	lea	hl, ix
	add	hl, bc
	ld	iy, (hl)
	ld	(iy), 0
	ld	bc, -192
	lea	iy, ix
	add	iy, bc
	ld	hl, (iy)
	push	hl
	pop	iy
	inc	iy
	lea	de, iy
	ld	bc, 89
	ldir
	ld	hl, _.str
	push	hl
	ld	hl, -327680
	push	hl
	call	_sprintf
	pop	hl
	pop	hl
	ld	hl, 10
	push	hl
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	hl, (hl)
	push	hl
	ld	hl, 294
	push	hl
	ld	hl, _asn1_demo
	push	hl
	call	_asn1_decode
	pop	de
	pop	de
	pop	de
	pop	de
	ld	bc, -195
	lea	iy, ix
	add	iy, bc
	ld	(iy), hl
	push	hl
	ld	hl, _.str.1
	push	hl
	ld	hl, -327680
	push	hl
	call	_sprintf
	pop	hl
	pop	hl
	pop	hl
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	iy, (hl)
	lea	iy, iy + 3
	or	a, a
	sbc	hl, hl
	ex	de, hl
BB0_1:
	ld	bc, -195
	lea	hl, ix
	add	hl, bc
	ld	hl, (hl)
	or	a, a
	sbc	hl, de
	jr	z, BB0_3
	or	a, a
	sbc	hl, hl
	ld	l, (iy - 3)
	push	ix
	ld	bc, -189
	add	ix, bc
	ld	(ix), de
	pop	ix
	ld	de, (iy)
	ld	bc, (iy + 3)
	push	bc
	push	de
	push	hl
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	hl, (hl)
	push	hl
	ld	hl, _.str.2
	push	hl
	ld	hl, -327680
	push	hl
	ld	bc, -198
	lea	hl, ix
	add	hl, bc
	ld	(hl), iy
	call	_sprintf
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	de, (hl)
	ld	bc, -198
	lea	iy, ix
	add	iy, bc
	ld	iy, (iy)
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	inc	de
	lea	iy, iy + 9
	jr	BB0_1
BB0_3:
	ld	hl, (ix - 72)
	ld	de, (ix - 75)
	ld	bc, 10
	push	bc
	ld	(ix - 3), de
	ld	de, -192
	lea	iy, ix
	add	iy, de
	ld	bc, (iy)
	push	bc
	ld	de, (ix - 3)
	push	de
	push	hl
	call	_asn1_decode
	push	hl
	pop	de
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	ld	bc, -192
	lea	hl, ix
	add	hl, bc
	ld	iy, (hl)
	lea	iy, iy + 3
	or	a, a
	sbc	hl, hl
	push	hl
	pop	bc
BB0_4:
	push	de
	pop	hl
	or	a, a
	sbc	hl, bc
	jp	z, BB0_6
	or	a, a
	sbc	hl, hl
	ld	l, (iy - 3)
	ld	(ix - 3), bc
	push	ix
	ld	bc, -192
	add	ix, bc
	ld	(ix), de
	pop	ix
	ld	de, (iy)
	ld	bc, (ix - 3)
	ld	(ix - 3), de
	push	ix
	ld	de, -189
	add	ix, de
	ld	(ix), bc
	pop	ix
	ld	bc, (iy + 3)
	push	bc
	ld	de, (ix - 3)
	push	de
	push	hl
	ld	bc, -189
	lea	hl, ix
	add	hl, bc
	ld	hl, (hl)
	push	hl
	ld	hl, _.str.2
	push	hl
	ld	hl, -327680
	push	hl
	ld	bc, -195
	lea	hl, ix
	add	hl, bc
	ld	(hl), iy
	call	_sprintf
	ld	de, -189
	lea	hl, ix
	add	hl, de
	ld	bc, (hl)
	ld	de, -195
	lea	iy, ix
	add	iy, de
	ld	iy, (iy)
	ld	(ix - 3), bc
	push	ix
	ld	bc, -192
	add	ix, bc
	ld	de, (ix)
	pop	ix
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	ld	bc, (ix - 3)
	inc	bc
	lea	iy, iy + 9
	jp	BB0_4
BB0_6:
	or	a, a
	sbc	hl, hl
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	section	.data,"aw",@progbits
	public	_asn1_demo
_asn1_demo:
	db	"0Ç""0", 13, "	*ÜHÜ˜", 13, "", 0, "Ç", 0, "0Ç", 10, "Ç", 0, "√-xx˙¯Ó’y(g+tˆ◊]B(íB\cN@ûü@6ÿûàË⁄ÅªOy6´Á a3+Ü^j|""’Uü‰ÇCÓg©3ü¡Kô±äÔaê~œ\#ß‰Í:QsóJêJ*J$", 13, "£<Ìx˙<ÏrÔ|¶5Øàë", 10, "ìK∂—SC(€i¥tºá›äk®R∏ÁwNÇ6»ﬂx‰ÿ", 0, "±2#Úøç≠Üñ†0ABDiﬁãù$1haSÉ˚5-»Ï", 10, ")o)%·Õ∫≈>ƒÅ°ºR^∆Ò£™π¸£ƒ1’7ÛO/]œµPÛ®›	Öﬂô/êãºoüÖ≈""^e†J&üˇŒ∏«?˙≠è", 0, ""

	section	.rodata,"a",@progbits
	private	_.str
_.str:
	db	10, 10, "----------------------------------", 10, "ENCODEX ASN.1 Decoder Demo", 10, 0

	section	.rodata,"a",@progbits
	private	_.str.1
_.str.1:
	db	"Decode complete, %u objects parsed.", 10, 0

	section	.rodata,"a",@progbits
	private	_.str.2
_.str.2:
	db	"Obj %u, Tag Id: %u, Size: %u, Addr: %u", 10, 0

	ident	"clang version 15.0.0 (https://github.com/jacobly0/llvm-project 72d82503a68765fd5a06d2026984f1fa399a54ea)"
	extern	__Unwind_SjLj_Register
	extern	__Unwind_SjLj_Unregister
	extern	__frameset
	extern	_sprintf
	extern	_asn1_decode
